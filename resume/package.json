{
  "name": "resume-builder",
  "private": true,
  "version": "1.0.0",
  "description": "Builds the resume HTML into a PDF using Puppeteer",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "puppeteer": "^24.0.0"
  }
}
